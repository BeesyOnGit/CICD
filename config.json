{
    "build": [{ "workDir": "/folder/where/to/execute/build/commande", "cmd": ["npm i", "npm run build"] }],
    "mouve": ["commande to copy the build folder t deploymeny folder ex : cp /home/CICD/portfolio/Client/dist /var/www/portfolio.com"],
    "clearFolder": "/home/CICD/portfolio // folder to remove after cloning and building",
    "repo": "https://github.com/BeesyOnGit/portfolio.git // repo to clone and watch"
}
